// Re-export new auth hooks for backward compatibility
export { useAuth } from '@/features/auth/api';

// Re-export theme hook
export { useThemeContext as useTheme } from '@/app/providers/theme-provider';
